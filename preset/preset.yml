name: Anchored Pro (opencode-go)
description: Pro 满血 × Minimal 工具面锚定——首轮只暴露 bash + str_replace_editor（Minimal 真实 schema），晋升后常驻发现工具 + 按需解锁；w6c persona（spec 句 + 分类指令，P11 实测 Pro 最优），rc.6 冷扫描晋升（anchored-standard 的 rc.6 迁移版）
order: 6
